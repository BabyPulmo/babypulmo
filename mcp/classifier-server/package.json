{
  "name": "@babypulmo/classifier-mcp",
  "version": "0.1.0",
  "description": "MCP server exposing Baby Pulmo's pediatric cough classifier + multi-modal severity + nearest-CHW routing so external clinical AI assistants can consume the same primitives.",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "babypulmo-classifier-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.16.5",
    "tsx": "^4.19.0",
    "typescript": "^5.5.4"
  }
}
